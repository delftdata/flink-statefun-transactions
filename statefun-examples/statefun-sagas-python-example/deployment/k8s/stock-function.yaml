---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stock-function
spec:
  selector:
    matchLabels:
      service: stock-function
  replicas: 2
  template:
    metadata:
      labels:
        service: stock-function
    spec:
      containers:
        - name: stock-function
          image: statefun-tpc-python-example_stock-function:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "0.25Gi"
              cpu: "100m"
            limits:
              memory: "0.25Gi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: stock-function
spec:
  type: ClusterIP
  selector:
    service: stock-function
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80